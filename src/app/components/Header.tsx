import React from 'react';

const Header = () => {
  return (
    <header className="h-[125px] bg-primary-200 flex flex-col items-center justify-center">
      <h2 className="h1">Header</h2>
    </header>
  );
};

export default Header;
