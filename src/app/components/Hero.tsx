import React from 'react';

const Hero = () => {
  return (
    <section className="h-screen bg-blue-300 flex flex-col items-center justify-center">
      <h2 className="h1">Hero</h2>
    </section>
  );
};

export default Hero;
